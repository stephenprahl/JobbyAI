services:
  # Override for development - only run postgres
  backend:
    profiles:
      - production  # This means backend won't start by default

  postgres:
    # Development PostgreSQL configuration
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=resume_plan_ai
    ports:
      - "5432:5432"
